<h1> <%= @director.name %> </h1>

<%= image_tag(@director.image, :class => 'feature') %>

<ul>
  <li><%= @director.nationality %></li>
</ul>

<% @director.films.each do |work| %>
  <%= link_to image_tag(film.image, :class => 'thumb'), film %>
<% end %>

<div class="controls">
  <%= link_to 'edit director', edit_director_path(@director)  %>
  <%= link_to 'delete director', director_path(@director.id), :method => 'delete', :data => { :confirm => 'sure?' } %>
</div>
